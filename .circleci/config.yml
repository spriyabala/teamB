version: 2.1


workflows:
  build-deploy:
    jobs:
      - build-deploy

jobs:

  build-deploy:
    machine: true
    steps:
      - checkout
      - run:
          name: build and push docker image to Heroku
          command: |
            set -x
            sudo curl https://cli-assets.heroku.com/install.sh | sh
            HEROKU_API_KEY=${HEROKU_API_KEY} heroku container:login
            HEROKU_API_KEY=${HEROKU_API_KEY} heroku container:push -a bibloteca web
            HEROKU_API_KEY=${HEROKU_API_KEY} heroku container:release -a bibloteca web
            
            

   
   





